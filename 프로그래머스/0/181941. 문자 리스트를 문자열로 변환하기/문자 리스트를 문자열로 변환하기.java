class Solution {
    public String solution(String[] arr) {
        String answer = "";
        for (int c = 0; c<arr.length; c++){
            answer = answer+arr[c];
        };
        return answer;
    }
}